export enum POINT_FEATURE {
	CHECKPOINT = 'checkpoint',
	CHECKPOINT_START = 'start checkpoint',
	CHECKPOINT_FINISH = 'finish checkpoint',
	SUMMIT = 'summit',
	WATER_ENTRY = 'water entry',
	WATER_EXIT = 'water exit',
	WATER_CHECKPOINT = 'water checkpoint'
}
